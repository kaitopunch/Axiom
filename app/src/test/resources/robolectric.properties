# Robolectric takes the SDK from the merged manifest when unit tests see resources; 34 is the newest
# android-all jar Robolectric 4.14 ships (36 is rejected outright).
sdk=34
# A bare Application: the tests build their own Axiom with Axiom.create() and never start Koin.
application=android.app.Application
