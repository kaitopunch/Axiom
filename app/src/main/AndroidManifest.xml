<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".DemoApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DemoAxiom">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.DemoAxiom">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- WorkManager uses its default initializer here (axiom/README.md, "WorkManager & multi-process",
             option a): the demo has one process, so nothing to opt out of. Axiom's worker has a plain
             (Context, WorkerParameters) constructor, so no WorkerFactory is registered. -->
    </application>

</manifest>
